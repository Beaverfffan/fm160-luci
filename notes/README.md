# notes/ —— 工作日志与长期索引

本目录是**工作记录**，不是设计文档。设计文档在 `docs/`；这里放的是
「当时为什么这么判」的原始依据，以及跨会话要用到的长期索引。

| 文件 | 内容 |
|---|---|
| `2026-09-01.md` … `2026-09-22.md` | 逐日工作日志（**全量**，不是节选）。含被推翻的结论、真机取证数据、判据与教训 |
| `MEMORY.md` | 长期速查索引（**跨设备**：面板、GL-BE14000、GL-MT5000 也在此）。**不设体积上限** |
| `archive/MEMORY-FULL-ARCHIVE.md` | 更长的开发档案。日志里的「存档 §NN」引用全部指向它 |
| `SMS-VERDICT-2026-09-22.txt` | FM160-CN 短信外发失败的判据定稿（含**已作废**的归因与订正） |
| `agent/SOUL.md`、`IDENTITY.md`、`USER.md`、`MEMORY.md` | 跨工作区延续用的身份/偏好文件（**PAT 已抹除**，见下） |

## ★ 引用约定：`存档 §NN`

`fm160-qmi/` 下的文件（含**已安装到设备上的 `usr/sbin/fm160-qmi`**，
它逐字节就是设备上跑的那一份）会引用「存档 §NN」。
自 2026-09-22 起该存档已随本目录发布：`archive/MEMORY-FULL-ARCHIVE.md`。

`fm160-qmi/usr/sbin/*` 与 `etc/*` **不做任何仓库专用改写**（不改注释、
不改路径），因为它们就是要装到设备上的东西 —— 仓里的副本与设备上的
运行件必须逐字节相同，否则「仓库里的源码」就不是「在跑的程序」了。
`fm160-qmi/README.md` 没有这个问题，所以它里面的引用已改成本仓可达的路径。

## ★ 日志里的路径是「当时的路径」

日志逐字保留案发时的样子，所以里面会出现工作区路径：
`_tools/istoreos-h69k/fm160-qmi/` —— 其内容即本仓的 `fm160-qmi/`。
同理 `_ref/fm160/docs/` 是厂商文档库在当时的落地位置，本仓不含该目录。

## 关于设备标识

本仓是公开仓库。**真实设备标识（IMEI、序列号、SIM 的 IMSI/ICCID）不入库**。

2026-09-22 的全量同步（脚本 `_tools/istoreos-h69k/290-publish-memory.py`）
在写入前对 19 个文件逐条替换并**复查**（不是替换完就算过）——
替换对象与计数：

| 类别 | 占位符 | 出现次数 |
|---|---|---|
| 模组 IMEI | `<IMEI>` | 9 |
| SIM IMSI | `<IMSI>` | 5 |
| SIM ICCID | `<ICCID>` | 5 |
| 个人手机号（收发测试对象） | `<手机号-A>` … `<手机号-D>` | 9 |
| GitHub PAT | `<GITHUB_PAT_REDACTED>` | 1 |

复查用保守扫描器（IMEI/IMSI/ICCID/11 位手机号/PAT/私钥）**零命中**。

**刻意保留**：`+8613344181200`（中国电信短信中心 SMSC）。它形式上像手机号，
但是运营商服务号、公开可查，而且是 `EF_SMSP` 解码自证链的关键证据 ——
抹掉它，那条「卡里有短信中心 == `AT+CSCA?` 原始应答」的互证就没了。
保留的还有实验室局域网地址（`192.168.x.x`，与 `docs/HARDWARE-PROBE.md` 写法一致）。

## 两处已作废的旧说法（别照抄）

1. **`MEMORY.md` 的「容量上限 6 KB」**：该约定已作废（用户 2026-09-21 明确）。
   速查条目直接写全，完整推导进 `archive/`。
2. **短信失败归因**：`SMS-VERDICT-2026-09-22.txt` 与 `2026-09-22.md` §14
   一度把根因判为「没有 IMS 承载 / 无 CS 域」。**该归因已作废** ——
   实际是**原厂（模组）固件缺陷，后期固件更新修复**（同文件卷首订正、日志 §15）。
   仍然成立的是它产出的**观测**：NR SA ≈95 ms 本地秒拒 vs LTE 满 40 s `T1_RP`、
   卡侧能力已排除、PDU/text 无差异。
